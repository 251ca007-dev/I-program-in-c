#include<stdio.h>
int digits (int s)
{
    int sum=0;
    while(s>0)
    {
        sum=sum+(s%10);
        s=s/10;
    }
    printf("sum of digits=%d",sum);
    return(sum);
}

int main()
{
    int n;
    printf("enter the value:");
    scanf("%d",&n);
    digits(n);
    return 0;

}

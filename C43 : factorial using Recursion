#include<stdio.h>
int fact(int n)
{
    if(n==0)
    return 1;
    else
    return n*fact(n-1);
}
int main()
{
    int num;
   printf(“Factorial Using Function –“);
    printf(“enter the number:\n”);
    scanf(“%d”,&num);
    printf(“the factorial of %d = %d”,num,fact(num));
    return 0;
    
}



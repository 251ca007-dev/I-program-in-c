#include<stdio.h>
int main()
{
    int n, largest;
    printf("enter the no:");
    scanf("%d",&n);
    int a[n];
    
    for(int i=0;i<n;i++)
    {
        scanf("%d",&a[i]);
        largest =a[0];
if(largest<a[i]){
            largest=a[i];
            
        }
    }
        printf("%d", largest);
return 0;
    }


